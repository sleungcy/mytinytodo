apiVersion: v1
kind: Service
metadata:
  name: {{ include "mytinytodo.fullname" . }}
  labels:
    {{- include "mytinytodo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mytinytodo.selectorLabels" . | nindent 4 }}